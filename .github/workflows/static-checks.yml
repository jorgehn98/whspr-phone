name: Static checks

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check-static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run static checks
        run: scripts/check-static.py
