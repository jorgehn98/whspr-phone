name: Static checks

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check-static:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run static checks
        shell: pwsh
        run: ./scripts/check-static.ps1
